
import {  parseTExp} from "./TExp" ;
// Function to print the components of a TExp
const printTExpComponents = (input: string): void => {
    const parsedTExp = parseTExp(input);
        //parseTExp=parseTExp.arguments
         //   log(parseTExp);
       
}
// Test the function with the given input
printTExpComponents("(inter number (inter boolean string))");


// tsc printTExpComponents.ts
// node printTExpComponents.js



